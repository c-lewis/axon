[package]
name = "expression"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = { version = "0.4.38", features = ["now", "arbitrary", "serde"] }

[dev-dependencies]
proptest = "1.5.0"
proptest-derive = "0.5.0"
